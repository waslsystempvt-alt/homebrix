/**
 * Initial launch markets. The homepage "Top Cities" section is scoped to just
 * these while the business proves out the Mumbai / Navi Mumbai / Thane belt —
 * every other city page still works via direct URL, search, and the sitemap,
 * it's just not promoted on the homepage yet. Add a slug here to feature it.
 */
export const LAUNCH_CITY_SLUGS = ["mumbai", "navi-mumbai", "thane"];
